<script lang="ts">

</script>

<div class="container">
    <div class="row">
        <div class="col">
            <h1>Path Routing</h1>
            <p>
                Path routing is <code>@wjfe/n-savant</code>'s default routing mode. It uses the path of the URL to 
                determine which routes to render.
            </p>
            <p>
                For this to work, regular links <strong>must</strong> be replaced with <code>&lt;Link&gt;</code> 
                components from <code>@wjfe/n-savant</code>. This is because the <code>&lt;Link&gt;</code> component 
                intercepts clicks and navigates to the new route without reloading the page.
            </p>
        </div>
    </div>
</div>